# 忽略 macOS 系統文件
.DS_Store
.AppleDouble
.LSOverride

# 编辑器/IDE
.idea/
.vscode/
*.swp
*.swo
*~
.notes

# Python / venv（如果未来添加 plugin_api.py）
__pycache__/
*.py[cod]
venv/
.pytest_cache/

# Node / build（不需要，因为我们是 IIFE）
node_modules/
dist/          # 注意：plugin/dist/ 是我们手动写的源码目录，不忽略
# 但如果我们用了打包工具，打包输出应该另放目录，例如 .build/

# 临时文件
*.log
.npm

# Hermes 本地目录（不提交到仓库）
# 这是用户本地的，不用管
# ~/.hermes/

# Design docs (local scratch)
design.md
